<?xml version="1.0" encoding="utf-8"?>
<resources>

    <declare-styleable name="PagerListTab">
        <attr name="tab_left_txt" format="string" />
        <attr name="tab_right_txt" format="string" />
        <attr name="tab_txt_size" format="dimension"/>
    </declare-styleable>

    <declare-styleable name="VerifyEditText">
        <!--输入框数量-->
        <attr name="inputCount" format="integer" />
        <!--输入框间距-->
        <attr name="inputSpace" format="dimension" />
        <!--下划线厚度-->
        <attr name="underlineHeight" format="dimension" />
        <!--输入框文本大小-->
        <attr name="mTextSize" format="dimension" />
        <!--下划线选中的颜色-->
        <attr name="focusColor" format="color" />
        <!--下划线未选中的颜色-->
        <attr name="defaultColor" format="color" />
        <!--下划线距文本的距离-->
        <attr name="underlineSpace" format="dimension" />
        <!--光标-->
        <attr name="cursorDrawable" format="reference" />
    </declare-styleable>

    <declare-styleable name="GradualChangeBgView">
        <!-- 开始颜色（底部颜色） -->
        <attr name="startColor" format="color" />
        <!-- 结束颜色（顶部颜色） -->
        <attr name="endColor" format="color" />
    </declare-styleable>

    <declare-styleable name="FlowNode">
        <attr name="bgColor" format="color" />
        <attr name="radius" format="dimension" />
    </declare-styleable>


</resources>